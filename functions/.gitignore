node_modules/
*.local
.runtimeconfig.json

# Environment files. .env.marching-art holds only the non-secret
# DATA_NAMESPACE and is committed intentionally; everything else may carry
# secrets and must never be committed.
.env
.env.*
!.env.marching-art
