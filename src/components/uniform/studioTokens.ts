// Shared class-string tokens for the Uniform Studio controls (split out so
// component files stay component-only for react-refresh).

export const LABEL = 'block text-[10px] font-bold text-muted uppercase tracking-wider mb-1';
export const SECTION_LABEL =
  'text-[10px] font-bold text-muted uppercase tracking-wider border-b border-line pb-1 mb-3';
